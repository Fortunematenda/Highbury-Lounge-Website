# Admin app

The Highbury Lounge administration portal currently runs inside the `web` package at:

**http://localhost:5173/admin**

It shares the same Cloudflare D1 database and vinext deployment as the public site.

This folder is reserved if you later split admin into its own deployable app. Until then, develop admin UI under `web/app/admin/`.
